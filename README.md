# Portfolio — Kamalesh K V

This is a minimal static portfolio site for Kamalesh K V. Edit `index.html` to update content further.

Contact
- Phone: 9629616965
- Email: kamalesh.kv2024csecs@sece.ac.in
- LinkedIn: https://www.linkedin.com/in/kamalesh-k-v

Preview locally

Windows (quick): open `index.html` directly in a browser.

Run a simple local server (recommended):

```powershell
cd "C:\Users\kamal\Downloads\portfolio"
python -m http.server 8000
```

Then open `http://localhost:8000`.

Next steps
- Replace placeholder project links with real URLs or screenshots (add an `assets/` folder).
- Add project pages or GitHub repo links for each project card.
- Optional: add a `resume.pdf`, images, and a GitHub Pages deploy workflow — say the word and I will add them.
Next steps
- Replace placeholder project links with real URLs or screenshots (add an `assets/` folder).
- Add project pages or GitHub repo links for each project card.
- `assets/` folder created with placeholder screenshots at `assets/project1.svg` and `assets/project2.svg`.
- `resume.html` created and a placeholder `resume.pdf` added; replace `resume.pdf` with your finalized PDF.
- A GitHub Actions workflow `.github/workflows/deploy.yml` was added to deploy the site to GitHub Pages on pushes to `main`.